Eingabe: A[1,n]
Ausgabe: MaxWin(A)
Algorithmus:
min := 0;
max := 0;
minT := 1;
maxT := 1;

i := 1;
while (i < n)
    if A[i] < min
        min := A[i]
        minT := i
    else
        if A[i] > max AND minT < maxT
        max := A[i]
        maxT := i
    i++

return (max-min)